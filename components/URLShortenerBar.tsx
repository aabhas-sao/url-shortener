import React from "react";
import ButtonPrimary from "./ButtonPrimary";
import Input from "./design-system/Input";

const URLShortenerBar = () => {
  return (
    <div>
      <Input />
      <ButtonPrimary>Shorten it!</ButtonPrimary>
    </div>
  );
};

export default URLShortenerBar;
