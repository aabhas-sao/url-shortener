import React from "react";
import Input from "../design-system/Input";
import URLShortenerBar from "../URLShortenerBar";

const Header = () => {
  return (
    <div>
      <h1>More than just shorter links</h1>
      <URLShortenerBar />
    </div>
  );
};

export default Header;
